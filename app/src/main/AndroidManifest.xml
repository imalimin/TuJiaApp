<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tujia" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".book.BookActivity" >
        </activity>
        <activity android:name=".book.BookListActivity" >
        </activity>
        <activity android:name=".instruction.InstructionActivity" >
        </activity>
        <activity android:name=".cartoon.CartoonActivity" >
        </activity>
        <activity
            android:name=".cartoon.MediaPaly_activity"
            android:configChanges="orientation|keyboardHidden|screenSize" >
        </activity>
        <activity android:name=".manhua.ManHuaActivity" >
        </activity>
        <activity
            android:name=".comic.reader.ReadingActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
